<!DOCTYPE html>
<html>
    <head>
        <title>Herbonomics</title>
    </head>
    <body>
        <h3>Growers Strains</h3>

        <form class="" action="/grower_supply/search" method="get">
            <label>Search Strain Name</label>
            <input type="text" name="search">
          <button type="submit" name="button">Search</button>
        </form>

        <table border = "1" cellpadding = "15">
            <tr>
                <th>Strain</th>
                <th>Phenotype</th>
                <th>THC %</th>
                <th>CBD %</th>
                <th>Price ($/lb)</th>
                <th>Clean Green Certified</th>
                <th>Grower</th>
            </tr>
            {% if strains is not empty %}
                {% for strain in strains %}
                <tr>
                    <td>
                        {{ strain.getStrainName }}
                    </td>
                    <td>
                        {{ strain.getPheno }}
                    </td>
                    <td>
                        {{ strain.getThc }}
                    </td>
                    <td>
                        {{ strain.getCbd }}
                    </td>
                    <td>
                        {{ strain.getPrice }}
                    </td>
                    <td>
                        {% if strain.getCgc == 1 %}
                            &#10004;
                        {% else %}
                            &#x2718;
                        {% endif %}
                    </td>
                    <td>
                        {{ strain.findGrowerName(strain.getGrowersId) }}
                        <!--How do I get the specific grower's name here?-->
                    </td>
                </tr>
                {% endfor %}
            {% else %}
            <tr>
                <td colspan = "7">
                     No Strains Available
                </td>
            </tr>
            {% endif %}
        </table>
            {% include '/includes/home_form.html.twig' %}
    </body>
</html>
